doctype html 
html(land="ko")
    head
        title #{pageTitle === undefined ? "noName" : pageTitle} | Wetube
        link(rel="stylesheet" href="https://unpkg.com/mvp.css")
    body 
        header 
            h1=pageTitle
            nav 
                ul 
                    li 
                        a(href="/videos/upload") Upload Video 
                    li 
                        a(href="/") HOME
                    li 
                        a(href="/search") SEARCH
        main
            block content
    include partials/footer
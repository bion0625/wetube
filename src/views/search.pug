extends base.pug
include mixins/video

block content
    form(method="GET").search__form
        input(placeholder="search by title", type="text", name="keyword")
        button
            i.fas.fa-search
    div.video__grid
        each item in videos
            +video(item) 
        else 
            sapn.empty__message No Video found